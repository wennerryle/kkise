export class Viewport {
    trackWidth = $state(300);
    totalSeconds = $state(600);
    currentTime = $state(0);
    zoomLevel = $state(100);
    isRecording = $state(false);
    scrollLeft = $state(0);
}
