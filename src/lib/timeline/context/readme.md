## Ответственность

Фасад, объединяющий state и logic. Единая точка входа для всех операций. Svelte-контекст для DI.

Правила:

- TimelineContext владеет экземплярами из state/
- Вызывает функции из logic/ для валидации и вычислений
- Мутирует state/ объекты
- Не знает о DOM
